---
description: Start a NEW project — plan its ICM structure by writing a PROJECT-BRIEF.md (classifies the type, asks only what you know, researches the tech for you).
argument-hint: "[one line about what you want to build]"
allowed-tools: [Skill]
---

Invoke the `project-brief` skill to plan a brand-new project from scratch.

If the user gave a description, use it as the starting goal: $ARGUMENTS

(If a codebase already exists and they want to add structure on top of it, use `/adopt-project`
instead.)
