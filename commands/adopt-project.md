---
description: Adopt an EXISTING repo into ICM — analyze the codebase and write a PROJECT-BRIEF.md (with an Existing Inventory) so the structure can be laid on top without moving any files.
argument-hint: "[path to the existing repo, or blank for current dir]"
allowed-tools: [Skill]
---

Invoke the `adopt-project` skill to analyze an existing codebase and generate its brief.

Repo path (or blank to use the current directory): $ARGUMENTS
