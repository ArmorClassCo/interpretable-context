---
description: Validate an ICM project's structure — runs the mechanical checks (unfilled slots, the L0/L1 split, router targets resolve, maps_to bindings, manifest well-formed, and brownfield no-touch) and reports any findings.
argument-hint: "[path to an ICM project, or blank for current dir]"
allowed-tools: [Skill]
---

Invoke the `icm-validate` skill to QA an ICM project against the registry.

Project path (or blank to use the current directory): $ARGUMENTS
