---
description: Capture this session's recurring learnings into the current ICM project's own context files (conventions, gotchas, decisions, commands). Keeps only what will recur; project-local only.
allowed-tools: [Skill]
---

Invoke the `session-learnings` skill to scan this session for reusable learnings and, with approval,
file them into the current project's context files. Project-local only — it never edits the skills,
the registry, or global config.

Optional focus from the user: $ARGUMENTS
